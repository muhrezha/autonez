# ============================================================
# AUTONEZ — Development Environment
# File ini di-commit ke git (tidak ada secrets di sini)
# ============================================================

NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_APP_ENV=development

# Maintenance mode: aktif otomatis di env development
MAINTENANCE_MODE=false
